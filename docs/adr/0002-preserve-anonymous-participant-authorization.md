# Preserve anonymous participant authorization

You Pick remains usable without registration: reads are public, while mutations require an authenticated anonymous participant identity. Suggestions can be removed only by their owner, and each participant can create or replace one vote per poll; the Edge Function verifies identity and enforces these ownership rules.
