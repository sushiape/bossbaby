name: Edge Function Release

on:
  workflow_call:
    inputs:
      deploy:
        description: Deploy validated Edge Functions to production.
        required: false
        default: false
        type: boolean
      expected_sha:
        description: Commit to validate and release.
        required: false
        default: ""
        type: string
      allow_historical_main:
        description: Allow an ordered dispatcher to release an earlier main commit.
        required: false
        default: false
        type: boolean
  workflow_dispatch:
    inputs:
      deploy:
        description: Deploy validated Edge Functions to production.
        required: true
        default: true
        type: boolean

concurrency:
  group: ${{ inputs.deploy && 'production-release-unit' || format('edge-validation-{0}', github.run_id) }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.expected_sha || github.sha }}
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.9.5
      - name: Check formatting
        run: deno fmt --config backend/supabase/deno.json --check backend/supabase/functions
      - name: Lint
        run: deno lint --config backend/supabase/deno.json backend/supabase/functions
      - name: Type-check function graph
        run: deno check --config backend/supabase/deno.json backend/supabase/functions/you-pick/index.ts
      - name: Run unit tests
        run: deno test --config backend/supabase/deno.json --allow-env backend/supabase/functions

  deploy:
    name: Deploy production
    if: inputs.deploy
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: Production
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ inputs.expected_sha || github.sha }}
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - uses: supabase/setup-cli@v3
        with:
          version: 2.114.0
      - name: Verify deployable main revision
        env:
          ALLOW_HISTORICAL_MAIN: ${{ inputs.allow_historical_main }}
        run: |
          set -euo pipefail
          git fetch --no-tags origin main
          if [[ "$ALLOW_HISTORICAL_MAIN" == "true" ]]; then
            git merge-base --is-ancestor HEAD origin/main
          else
            test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"
          fi
      - name: Deploy all Edge Functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: supabase --workdir backend functions deploy --project-ref "$SUPABASE_PROJECT_REF"
      - name: Smoke-test You Pick read endpoint
        env:
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          set -euo pipefail
          endpoint="https://${SUPABASE_PROJECT_REF}.supabase.co/functions/v1/you-pick/suggestions?limit=1"
          for attempt in {1..12}; do
            if curl --fail --silent --show-error "$endpoint" >/dev/null; then
              exit 0
            fi
            sleep 5
          done
          echo "Edge Function smoke test failed after 12 attempts." >&2
          exit 1
